# Passing payloads (DMSACC evidence)

Populated after the first Trade Test submission with **0 CDS validation errors** (DMSACC).

| File | When |
|------|------|
| `trade-test-cds-v2-request.xml` | Copy of exact XML sent on the accepting submit |
| `trade-test-cds-v2-response.xml` | HMRC acceptance response |
| `scenario-summary.json` | LRN, MRN, conversation ID, timestamp |

Also copy the request XML to `spec/passing-payload.xml` as the repo regression baseline.
